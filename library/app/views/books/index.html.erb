<h1>All Books</h1>
<ol>
  <% @books.each do |book| %>
  <%= image_tag book.image_cover_url %>  
  <li><%= link_to(book.title, book) %> by <%= book.author_id %><br />
  	</li>
<% end %>
</ol>
<%= paginate @books %>
